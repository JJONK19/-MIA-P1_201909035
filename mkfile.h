#ifndef MKFILE
#define MKFILE

#include <iostream>
#include <regex>
#include <stack>

#include "structs.h"
#include "mkdir.h"

void mkfile(std::vector<std::string> &parametros, std::vector<disco> &discos, usuario &sesion);

#endif