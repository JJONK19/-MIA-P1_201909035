#ifndef MKFILE
#define MKFILE

#include <iostream>
#include <regex>
#include <stack>
#include <string.h>
#include <fstream>

#include "structs.h"
#include "mkdir.h"

void mkfile(std::vector<std::string> &parametros, std::vector<disco> &discos, usuario &sesion);

#endif