#ifndef CHOWN
#define CHOWN

#include <iostream>
#include <regex>
#include <stack>

#include "structs.h"

void chown(std::vector<std::string> &parametros, std::vector<disco> &discos, usuario &sesion);

#endif